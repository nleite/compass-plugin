'use strict';

var Reflux = require('reflux');

var M034Actions = Reflux.createActions([
/**
 * define your actions as strings below, for example:
 */
'toggleStatus', 'loadHomework']);

module.exports = M034Actions;